"""Template assets for the custom authorized-target SDK (#33).

Each subdirectory (``c``, ``cpp``, ``swift``, ``objc``) is a plain-text
template project: a byte-input harness source, a default
``target-manifest.json`` (with a ``{name}`` placeholder), and a README
snippet. :func:`ios_research.targetsdk.init_template` materializes them into a
user project directory; nothing in this package is executable code.
"""
